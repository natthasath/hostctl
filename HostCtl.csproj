<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net6.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>

    <!-- Make the binary name lowercase -->
    <AssemblyName>hostctl</AssemblyName>
    <RootNamespace>hostctl</RootNamespace>

    <!-- Version info used by version -->
    <Version>2.0.0</Version>
    <FileVersion>2.0.0.0</FileVersion>
    <InformationalVersion>2.0.0</InformationalVersion>

    <!-- Don't let the SDK append +<git-commit-hash> to InformationalVersion -->
    <IncludeSourceRevisionInInformationalVersion>false</IncludeSourceRevisionInInformationalVersion>
  </PropertyGroup>

  <ItemGroup>
    <!-- no external CLI libraries (no System.CommandLine) -->
  </ItemGroup>

</Project>
